plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    // 替换为你的项目包名，必须和AndroidManifest一致
    namespace "com.your.iptv.packagename"
    compileSdk 34 // 稳定版，适配Android 14
    buildToolsVersion "34.0.0"

    defaultConfig {
        applicationId "com.your.iptv.packagename" // 替换为你的项目包名
        minSdk 21 // 兼容绝大多数电视盒子，可根据需求调整
        targetSdk 34 // 适配Android 14
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        // 解决清单合并冲突的占位符
        manifestPlaceholders = [
            "appAuthRedirectScheme": "com.your.iptv.packagename"
        ]
    }

    buildTypes {
        release {
            // 正式包开启混淆，可根据需求调整
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            // 开启资源压缩，减小APK体积
            shrinkResources true
        }
        debug {
            minifyEnabled false
            debuggable true
        }
    }

    // ==================== 核心：Java 17 编译配置（解决JDK兼容问题） ====================
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
        // 开启核心库反序列化，解决Media3兼容问题
        coreLibraryDesugaringEnabled true
    }

    kotlinOptions {
        jvmTarget = '17'
    }

    // ==================== 视图绑定（DataBinding）配置 ====================
    buildFeatures {
        viewBinding true
        dataBinding true
    }

    // ==================== 清单合并规则（彻底解决冲突） ====================
    lint {
        abortOnError false
        checkReleaseBuilds false
    }
}

dependencies {
    // ==================== 核心依赖：Media3 替代旧版ExoPlayer ====================
    // 官方维护，彻底解决AndroidX/支持库冲突
    implementation "androidx.media3:media3-exoplayer:1.3.1"
    implementation "androidx.media3:media3-ui:1.3.1"
    implementation "androidx.media3:media3-common:1.3.1"
    implementation "androidx.media3:media3-session:1.3.1" // 后台播放必需

    // ==================== AndroidX 核心依赖 ====================
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'androidx.legacy:legacy-support-v4:1.0.0'

    // ==================== 核心库反序列化（兼容低版本Android） ====================
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.0.4'

    // ==================== 测试依赖 ====================
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}
